<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Campaign__c</fullName>
    <label>Campaign</label>
    <type>Lookup</type>
    <referenceTo>Campaign</referenceTo>
    <relationshipLabel>Meetings</relationshipLabel>
    <relationshipName>Meetings</relationshipName>
    <deleteConstraint>SetNull</deleteConstraint>
    <required>false</required>
    <description>The originating trade-show Campaign, matching the Zoho Meetings.Campaign lookup. Uses the standard Salesforce Campaign object rather than a custom one.</description>
</CustomField>
