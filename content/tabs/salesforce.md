---
tab: "salesforce"
title: "Salesforce"
route: "#/salesforce"
items: 2
source:
  lede: "src/app.html · vSF()"
  items: "src/app.html · SF_ITEMS"
---

# Salesforce

## Лид страницы

The same discipline, a different platform Salesforce Not a second specialisation — proof that the discipline underneath the Zoho work is not tied to one vendor’s SDK. One widget ported end to end, and one sync pattern rebuilt on Salesforce primitives, both offline: an SFDX project with real Apex and a real Lightning Web Component sits in the repository, and the interactive parts of this site run against a browser stand-in, exactly like every other platform demo here. Status Two pages, deliberately narrow rather than broad: sf-lwc is a real interactive port with a working demo; sf-sync is a write-up comparing the underlying decision, not a second demo of the same sync mechanics already live in the Zoho platform emulator. The format is the same as everywhere else — the constraint, what was built, and the trade , stated plainly.

## Карточки

| id | Заголовок (`t`) | Подпись (`s`) | Вид |
| --- | --- | --- | --- |
| [`sf-sync`](../pages/sf-sync.md) | Apex vs Deluge: the same sync, twice | The delta-sync pattern rebuilt on Salesforce primitives. | note |
| [`sf-lwc`](../pages/sf-lwc.md) | One screen, two platforms | The employment-history widget, ported to a Lightning Web Component, next to the Zoho original. | live |
