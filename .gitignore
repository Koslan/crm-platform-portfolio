node_modules
dist/
data/dataset.json
data/dataset.slim.json
*.png
*.tar.gz
.DS_Store

# scratch from a fidelity pass: backups, cut-out blocks, working notes.
# The notes quote the original org's module and field names, so they stay on disk.
*.bak-before-*
_to_delete/
# Notes from a fidelity pass quote the original org's modules, fields and domains
# verbatim. Match them by shape rather than by name: the last one landed in claude/
# instead of docs/ and the by-name rule missed it.
docs/fidelity-*.md
!docs/fidelity-map.md
claude/
*fidelity-*notes*.md
*.orig

# the all-sections build (npm run build:all)
dist-all/
